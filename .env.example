PORT=8080
CLIENT_URL=

# Redis — use the Redis protocol URL from Upstash dashboard (rediss://...)
REDIS_URL=

# Neon PostgreSQL
DATABASE_URL=

# Must match AUTH_SECRET in the Next.js app (auth.js v5 name)
AUTH_SECRET=
